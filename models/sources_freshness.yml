version: 2

sources:
  - name: main
    database: RLS_DM_DB
    schema: MAIN
    tables:
      - name: raw_orders
        #config:
        #  loaded_at_field: order_date    ## this column has to be timestamp or datetime format, not date
        #  freshness:
        #    warn_after: {count: 1, period: day}
        #    error_after: {count: 2, period: day}